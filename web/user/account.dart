// file: account.dart

class Account {
  // Currently pretty much a dummy class, used to load different heros to the stage
  String username; // The username of the account
  Account(this.username);
}